# PostgreSQL connection URL. Runtime and Docker Compose inject this value.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP listen port for the backend API.
PORT=8080

# Fallback HTTP listen port used when PORT is not set.
APP_PORT=8080
